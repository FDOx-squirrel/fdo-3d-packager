# MD.cff / CITATION.cff are YAML (ingest/mdcff steps).
pyyaml

# Sketchfab Data/Download API (fetch step, S2).
requests

# Validate MD.cff against fdo-squirrel's MD.cff-schema.yaml (mdcff step, S5;
# same library fdo-squirrel itself uses for the same schema, see its own
# requirements.txt).
jsonschema

# Run dist/<slug>.zip through fdo-squirrel (build_fdo step, S7) instead of
# reimplementing RDF generation ourselves -- same pattern and same pinning
# reasoning as fdo-squirrel-registry's own requirements.txt (S8): an
# unpinned main follows whatever lands there next, and a classification or
# generator change would silently reshape every future fdo-metadata.ttl
# this step produces (PRIMER.md A4, "Gepinnte Laufzeit"). Invoked as the
# installed console script, not imported -- see step_build_fdo.py.
fdo-squirrel @ git+https://github.com/FDOx-squirrel/fdo-squirrel.git@3868e1ef2a09f6adfcbb1e38b5d5cdb360817a13

# Not pip-installable, external requirements documented in README.md:
#   - Blender (headless), for the convert step (S3)
#   - nxsbuild / nxscompress from cnr-isti-vclab/nexus, for the nexus step (S4)
